<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="test.cj.com.myapplication">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"></uses-permission>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"></uses-permission>

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".fragment.FragmentTestActivtiy">

        </activity>
        <activity android:name=".activity.TestActivityA">

        </activity>
        <activity
            android:name=".activity.TestActivityB"
            android:launchMode="singleTask"></activity>
        <activity
            android:name=".MainActivity"
            android:launchMode="singleTask"></activity>

        <!--<service android:name="org.eclipse.paho.android.service.MqttService" />-->
        <!--<service android:name=".MQTTService" />-->

        <activity
            android:name=".LoadingActivity"
            android:theme="@style/Fullscreen">

        </activity>
        <activity android:name=".mcdb.EncryptedDBActivity">

        </activity>
        <activity android:name=".service.ServiceActivity">

        </activity>

        <service android:name=".service.MyService"></service>

        <activity android:name=".hanlder.HanlderActivity"></activity>
        <activity android:name=".AsyncTask.AsyncTaskActivity">


        </activity>

        <activity android:name=".webView.WebViewActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
    </application>

</manifest>